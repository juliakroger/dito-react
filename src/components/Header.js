import React from 'react';
import { Layout } from 'antd';

const Header = () => {
  return (
      <Layout.Header>
        <p style={{color: 'white'}}>Timeline de compras</p>
      </Layout.Header>
  );
};

export default Header;